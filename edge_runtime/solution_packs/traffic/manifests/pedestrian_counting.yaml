app_id: pedestrian_counting
solution_pack: traffic
version: 0.1.0
description: Count pedestrians from detector tracks and configured lines.
requires:
  data:
    - pedestrian_tracks
    - line_geometry
models:
  - vehicle_detector
produces:
  events:
    - pedestrian_count_event
hardware:
  preferred:
    vehicle_detector: GPU
    pedestrian_counting_app: CPU
